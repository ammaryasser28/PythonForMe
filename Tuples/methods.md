## 🧮 Tuple Methods

### count()
Counts how many times a value appears.
```python
t = (1, 2, 2, 3)
print(t.count(2))  # 2
```

### index()
Returns the index of a value.
```python
t = ("A", "B", "C")
print(t.index("B"))  # 1
```

